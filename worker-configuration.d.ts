/// <reference types="@cloudflare/workers-types" />

/**
 * Cloudflare Worker bindings for this project.
 *
 * This project currently does not declare KV, D1, R2, or other bindings.
 * Regenerate this file with `wrangler types` when bindings are added.
 */
interface Env {}
